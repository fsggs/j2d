/**
 * Assets Loader
 *
 * @class AssetsLoader
 * @exports module:loaders/AssetsLoader
 *
 * @constructor
 */
export default class AssetsLoader {
    progress = 0;

    getProgress() {
        return this.progress;
    }

    load() {
    }

    isLoaded(index) {
    }

    dispose(index) {
    }
}
